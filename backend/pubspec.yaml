name: tracker_api
description: Student Finance Tracker API — Dart Frog backend
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  dart_frog: ^1.1.0
  dart_frog_auth: ^1.2.2
  postgres: ^3.4.0

dev_dependencies:
  dart_frog_lint: ^0.1.0
  mocktail: ^1.0.4
  test: ^1.25.8
