name: tracker_mobile
description: Student Finance Tracker — Flutter mobile client
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_appauth: ^8.0.3
  flutter_secure_storage: ^10.3.1
  http: ^1.2.2
  intl: ^0.19.0
  provider: ^6.1.5+1
  jwt_decoder: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
